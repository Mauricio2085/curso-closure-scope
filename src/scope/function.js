function greeting() {
    let userName = 'Ana'; // Declaración de variable de función o local
    console.log(userName);

    if(userName === 'Ana') {
        console.log(`Hello ${userName}!`)
    }
}

greeting();
console.log(userName);